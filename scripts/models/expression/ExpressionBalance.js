'use strict';

class ExpressionBalance {
    constructor(source, target, preview) {
        this.source = source;
        this.target = target;
        this.preview = preview;
        Object.freeze(this);
    }
}
